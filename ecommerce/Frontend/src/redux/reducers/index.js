const initialState = {
   
};

export default function combineReducers(state = initialState, action) {
    const { type, payload } = action;

    switch(type) {
    
        default:
            return state
    };
}